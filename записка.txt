Калькулятор на pyqt с применением базы данных и QtDesigner

!!!второе число отрицательным делать нельзя!!!

- работа с вещественными числами
- косинусы, синусы углов до 180 градусов(sin(), cos()) - 
число округляется до целого с помощью round
стандартные действия:
1) a + b
2) a - b
3) a * b
4) a / b
- остаток по модулю (a % b)
- факториал числа до 2147483647(a!)
- корень числа

справа показываются предыдущие действия(при помощи sqlite3)

кнопка выключения off(sys.exit())
кнопка сброса (CE)
